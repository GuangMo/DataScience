Percentile can be directly linked to CDF by: F(x_v)=v  for example: x^2=4*.5=2, where F(x) = x^2 / 4 at 50th percentile 

The expression P(D|+) is called the positive predictive value. Similarly, P(~D|-), is called the negative predictive value